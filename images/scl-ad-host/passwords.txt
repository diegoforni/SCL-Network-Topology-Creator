# ============================================================
# SC.LOCAL — Domain Service Password Vault
# ------------------------------------------------------------
# This file is the protected "passwords data blob" hosted on the domain controller.
# It is served only via the restricted SMB share \\dc01\passwords$ (guest ok = no),
# whose `valid user` is the compromised service account svc_sql (Kerberoast target).
# Retrieving it proves the AD was compromised end-to-end.
# ============================================================

LAB_FLAG=FLAG{kerberoast-leads-to-ad-secret-blob-7f3a9c2e}

# Service account credentials rotated into the vault
svc_backup_db     : Backup#2024!
finance_service   : F1nance$Lab
sa_sql_admin      : SqLr00t#2024
hr_payroll_svc    : Payr0ll!SC
backup_exec       : BkExec#2024

# Note: rotate svc_sql next quarter (its SPN ticket is Kerberoastable).
